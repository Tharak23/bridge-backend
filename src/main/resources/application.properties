spring.application.name=Bridge
spring.profiles.include=local
server.port=${PORT:8080}

# Supabase Postgres (password in application-local.properties)
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.jlnxrpeyfubwfptitwhe
spring.datasource.password=${SUPABASE_DB_PASSWORD:}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=none
spring.jpa.open-in-view=false

# Clerk JWT — default URL; BridgeApplication.main sets a system property from .env CLERK_JWKS_URI so stale shell/IDE env cannot override to an old instance (e.g. gentle-elk-83).
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=https://concrete-kit-81.clerk.accounts.dev/.well-known/jwks.json
